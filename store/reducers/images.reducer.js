import { CUR_SET } from './data/sets';

const initialState = {
    data: CUR_SET.images,
};

export const imagesReducer = (state = initialState, action) => {
    switch (action.type) {

        default:
            
            return state;
    }
};
