import { CUR_SET } from './data/sets';

const initialState = {
    data: CUR_SET.traits,
};

export const traitsReducer = (state = initialState, action) => {
    switch (action.type) {

        default:

            return state;
    }
};
